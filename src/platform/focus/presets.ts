import type { FocusSectionScrollConfig } from './types';

export const CONTENT_FIRST_ROW_SCROLL: FocusSectionScrollConfig = {
  mode: 'comfort-zone',
  anchor: 'section-start',
  preserveHeaderWhenFirstRowFocused: true,
};
